import { Routes } from '@angular/router';
import { DashboardComponent } from './components/dashboard-despacho-web/dashboard-despacho-web';

export const routes: Routes = [
    {
        path: 'Dashboard',
        component: DashboardComponent
    }
];
